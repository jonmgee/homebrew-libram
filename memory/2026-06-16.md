# 2026-06-16 — Infrastructure scaffold + React scaffold + DB schema complete

- Created `~/homebrew-libram/` as a standalone project folder (sibling to `~/combat-tracker/`).
- Initialised with README.md, .gitignore, memory/ folder, and HANDOFF.md.
- Git repo initialised; first commit: `chore: initialise project structure`.
- **GitHub:** Public repo created at https://github.com/jonmgee/homebrew-libram — all commits pushed.
- **Supabase:** Project created at lebticyakvkencowjxmb.supabase.co — connection confirmed. Credentials stored in local `.env` and Vercel env vars.
-   **Database schema:** `entries` table created with shared fields (id, name, type, description, source, dm_only, tags, campaign, created_at) + JSONB `properties`. Indexes for full-text search, tags, properties, type, campaign, dm_only, created_at. RLS: anon = SELECT, authenticated = full CRUD.
- **Vercel:** Project created at https://homebrew-libram.vercel.app — placeholder live. Environment variables set for all three scopes. Git auto-deploy connected.
- **React scaffold:** package.json (React 19, Vite 8, Tailwind CSS v4, Supabase JS), config files, app shell, Supabase client — all created and verified.
- **Status:** All infrastructure, scaffold, and database schema complete. Awaiting first feature brief from Jon.