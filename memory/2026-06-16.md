# 2026-06-16 — Infrastructure scaffold + React scaffold complete

- Created `~/homebrew-libram/` as a standalone project folder (sibling to `~/combat-tracker/`).
- Initialised with README.md, .gitignore, memory/ folder, and HANDOFF.md.
- Git repo initialised; first commit: `chore: initialise project structure`.
- **GitHub:** Public repo created at https://github.com/jonmgee/homebrew-libram — all commits pushed.
- **Supabase:** Project created at lebticyakvkencowjxmb.supabase.co — connection confirmed (auth endpoint 200, API reachable). Credentials stored in local `.env` and Vercel env vars.
- **Vercel:** Project created at https://homebrew-libram.vercel.app — placeholder page deployed and live. Environment variables (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY) set for all three scopes. Git auto-deploy connected on push to main.
- **React scaffold:** package.json (React 19, Vite 8, Tailwind CSS v4, Supabase JS), config files (vite, tsconfig, vercel), app shell (main.tsx, App.tsx, index.css), Supabase client (src/lib/supabase.ts) — all created.
- **Build verified:** `npm install` (47 packages, 0 vulnerabilities), `npm run build` (clean, 0 errors), `npm run dev` (localhost:5173).
- **Deployment:** Scaffold committed (`08126b8`), pushed to main, Vercel auto-deployed — placeholder home screen live at https://homebrew-libram.vercel.app.
- **Status:** All infrastructure and scaffold complete. Awaiting first feature brief from Jon.